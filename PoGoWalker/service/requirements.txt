# Core service
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7

# Device pipe (iOS 26 / RSD tunnel). Optional at dev time: the service falls
# back to the mock device when this import is unavailable.
pymobiledevice3>=7.0.1

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
